pipeline {
    agent {
        label 'kubeagent'
    }

    stages {
        stage('build') {
            steps {
                script {
                    echo "Building the project..."
                    sh 'mvn clean package'
                }
            }
        }
    }
}
